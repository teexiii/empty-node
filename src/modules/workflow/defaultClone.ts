export default async function defaultClone(dir: string) {
    try {
    } catch (error) {
        console.error(`defaultClone error`, error);
        throw new Error('Default Clone');
    }
    console.log('Done Good Luck');
}
